# 本地仓库
## Git命令
```bash
#常见命令
git init
git add 本地仓库.md
git status
git commit -m "第一次提交"
git commit -v #回顾刚刚干了什么
git log
#提交更改
git add .
git commit -v
git commit -av
#版本回退
git reset --hard 513eb43
git reflog
```